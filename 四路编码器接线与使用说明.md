# 四路编码器接线与使用说明

> 当前映射和方向已经通过履带车直行数据确认。四路编码器参与左右履带速度测量，直行时为速度同步 PI 提供反馈。

## 1. 编码器映射

| 编码器 | 定时器 | A 相 | B 相 | 履带归属 | 直行前进原始计数 |
|---|---|---|---|---|---|
| ENC1 | TIM2 | PA15 | PB3 | 左履带 | 递减 |
| ENC2 | TIM3 | PB4 | PB5 | 左履带 | 递减 |
| ENC3 | TIM4 | PB6 | PB7 | 右履带 | 递增 |
| ENC4 | TIM8 | PC6 | PC7 | 右履带 | 递增 |

四路输入均配置为 `GPIO_NOPULL`，定时器均使用 Encoder TI12 正交 x4 计数模式和 16 位计数周期。

## 2. 软件计数

固件启动时将 TIM2、TIM3、TIM4、TIM8 清零并启动全部编码器通道。主循环读取各定时器计数值，根据 16 位回绕差值累加到四个有符号长期计数：

- `Encoder1_Count`
- `Encoder2_Count`
- `Encoder3_Count`
- `Encoder4_Count`

左右履带速度计算时，ENC1/ENC2 取反后合成为左侧速度，ENC3/ENC4 保持正向后合成为右侧速度。

## 3. 距离与速度归一化

四路编码器使用实车负载行驶数据进行距离尺度归一化：

| 编码器 | 归一化系数 |
|---|---:|
| ENC1 | `794/1000` |
| ENC2 | `869/1000` |
| ENC3 | `1013/1000` |
| ENC4 | `988/1000` |

左右侧各取两路归一化增量的平均值。速度以 20 ms 为采样基准，并使用 1/2 一阶低通；右侧参考速度再乘以 `1222/1024` 后进入左右速度差 PI。

## 4. 直行速度同步

CH1 位于转向死区、CH3 油门命令达到 60 us 且平均履带速度达到 300 counts/s 时，速度同步 PI 运行：

- 速度误差死区：50 counts/s。
- `Kp = 0.020 us/(count/s)`。
- `Ki = 0.0015 us/采样`。
- 积分限幅：±25000。
- PWM 修正限幅：±60 us。

修正量对左右履带等量反向叠加；前进和倒车均按速度幅值修正。CH1 进入转向控制时，PI 状态复位。

## 5. 串口观察

USART1 使用 PA9 TX、115200 8N1，每 100 ms 输出：

```text
CH1,CH2,CH3,CH4,CH5,CH6,LPWM,RPWM,ENC1,ENC2,ENC3,ENC4
```

最后四列对应四路有符号累计编码器计数，可直接观察每一路的归属、方向和累计变化。

## 6. 已确认的实车结果

- PE9 / TIM1_CH1 驱动左履带，PE11 / TIM1_CH2 驱动右履带。
- ENC1、ENC2 属于左履带，直行前进时原始累计值递减。
- ENC3、ENC4 属于右履带，直行前进时原始累计值递增。
- 四路编码器计数、USART1 12 列输出和直行速度同步已通过真机验收。
