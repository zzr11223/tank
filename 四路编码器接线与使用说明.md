# STM32F103ZET6 四路编码器版本

## 保持不变的原有引脚

- 原 6 路 PWM 输入：`PA2、PA3、PA6、PA7、PB0、PB1`
- 电机 PWM 输出：`PE9、PE11`
- USART1：`PA9、PA10`
- SWD 调试：`PA13、PA14`
- `PA0、PA1` 不使用

## 新增编码器引脚

| 编码器 | A 相 | B 相 | 硬件计数器 |
|---|---|---|---|
| 编码器 1 | PA15 | PB3 | TIM2 |
| 编码器 2 | PB4 | PB5 | TIM3 |
| 编码器 3 | PB6 | PB7 | TIM4 |
| 编码器 4 | PC6 | PC7 | TIM8 |

编码器为 AB 两相正交输入，使用定时器硬件计数；主循环将 16 位定时器计数扩展为 32 位累计计数。串口每 100 ms 输出一次 `ENC1`～`ENC4`，并输出最终写入左右履带 PWM 通道的 `R_PWM`、`L_PWM` 脉宽。

履带控制映射：`CH3` 控制前进/后退，`CH1` 控制转向；`CH2` 不再参与履带前后控制。直行或后退时，ENC1/ENC2 的右侧平均速度与 ENC3/ENC4 的左侧平均速度进入同步 PID；转弯时同步修正关闭。串口中的 `LS`、`RS` 为左右平均速度（计数/秒），`PID` 为当前 PWM 修正量。

## 原有 PWM 输入采集

原有 6 个输入脚仍然不变，底层改为 GPIO 双边沿 EXTI 加 TIM5 的 1 MHz 时间基准，以便释放 TIM2/TIM3 给编码器硬件计数。外部接线和失联保护逻辑保持不变。

## 电平注意

编码器工作电源为 5 V。接入前确认 A/B 输出和开发板输入电平兼容；优先使用 3.3 V 上拉或电平转换，并确保编码器、STM32 和信号源共地。

Keil 工程：`MDK-ARM\stm32zet6.uvprojx`
